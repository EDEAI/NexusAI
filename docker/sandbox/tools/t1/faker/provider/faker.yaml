extra:
  python:
    source: provider/faker.py
identity:
  author: nexusai
  description:
    en_US: Quickly generate a set of mock data (name, address, email, credit card
      number, etc.).
    pt_BR: Gere rapidamente um conjunto de dados fictícios (nome, endereço, e-mail,
      número de cartão de crédito, etc.).
    zh_Hans: 快速生成一组模拟数据（姓名、地址、邮件、信用卡号等）。
  icon: icon.jpg
  label:
    en_US: faker
    pt_BR: faker
    zh_Hans: faker
  name: faker
tools:
- tools/faker.yaml
